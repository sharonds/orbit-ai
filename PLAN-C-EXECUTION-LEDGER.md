# Plan C Execution Ledger

Plan: `docs/superpowers/plans/2026-04-25-plan-c-codex-followups.md`
Branch: `codex/plan-c-followups`

## Scope Rules

- No Claude branch names, `.claude/worktrees`, or Claude memory files.
- No out-of-scope dependency upgrades.
- No release-gate claim without runtime evidence.

## Task Evidence

| Task | Status | Commit | Runtime Evidence | Review Evidence | Notes |
|---|---|---|---|---|---|
| 1 Branch + ledger | Done | 18ab74b | `git switch -c codex/plan-c-followups origin/main`; `git status --short --branch`; `git diff --name-only`; `git diff --cached --name-only` | Controller review | Fresh execution branch from `origin/main`; plan artifact copied from committed planning branch via no-commit cherry-pick. |
| 2 Journey 8 honesty | Done | bc01a5b, ea3184c, 4d3ada5 | `PATH="/Users/sharonsciammas/.nvm/versions/node/v22.22.0/lib/node_modules/corepack/shims:/Users/sharonsciammas/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm -F @orbit-ai/e2e test src/journeys/08-migration-preview-apply.test.ts` passed after final fix | Spec/code review by subagents; final review over `18ab74b..4d3ada5` found no Critical/High/Medium/Low findings | Fixed exact stub shape, removed unrelated field setup, and updated current release docs so Journey 8 does not claim migration safety. |
| 3 API listener harness | Done | 8c70fc0, ca1b754, ee62091 | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/harness/api-server.test.ts`; `node --test scripts/release-workflow.test.mjs` passed under Node 22 | Code-quality review by subagents; final review over `ad28cee..ee62091` found no Critical/High/Medium/Low findings | Added local HTTP listener, runtime bind/forward/close proof, explicit bind-failure cleanup, Set-Cookie preservation, and exact-optional RequestInit handling. |
| 4 Journey 15 tenant isolation | Done | 96a0a2a, dd93ec1, 74e028e | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/15-tenant-isolation.test.ts` passed under Node 22 with SQLite | Tenant-safety/API parity reviews; final review over `d5de084..74e028e` found no findings | Uses one shared datastore, real CLI API server, beta-side no-side-effect postconditions, and exact-ID MCP search. Local Postgres run deferred to Task 7/15. |
| 5 Journey 11 MCP tools | Done | 9e09a7b, 36add79, eaff438 | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/11-mcp-core-tools.test.ts src/journeys/15-tenant-isolation.test.ts` passed under Node 22 | MCP tool-envelope review; final review over `d0d75f2..eaff438` found no Critical/High/Medium/Low findings | Invokes expected core MCP tools, enforces JSON tool envelopes, proves update/delete/move persistence, and keeps protocol-level schema validation separate. |
| 6 CRUD update persistence | Done | 2ce9471 | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/03-crud-contacts.test.ts src/journeys/04-crud-companies.test.ts src/journeys/05-crud-deals.test.ts` passed under Node 22 | Spec/code review found no Critical/High/Medium/Low findings | Plan field names corrected to current public contract: contacts/companies/deals assert `name`; contacts do not expose `last_name`, and deal `title` serializes as public `name`. |
| 7 Adapter-aware CLI workspace | Done | 3ae4386, 32811cd | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/harness/postgres-safety.test.ts`; `pnpm -F @orbit-ai/e2e test src/journeys/07-custom-field.test.ts src/journeys/08-migration-preview-apply.test.ts`; `ORBIT_E2E_ADAPTER=postgres DATABASE_URL=postgres://localhost:5432/orbit_e2e pnpm -F @orbit-ai/e2e test src/journeys/07-custom-field.test.ts src/journeys/08-migration-preview-apply.test.ts`; `node --test scripts/release-workflow.test.mjs` passed under Node 22 | Database/tenant-safety review found a High URL override issue in `postgres-safety.ts`; fixed in 32811cd with query-param rejection and regression coverage | CLI workspace now selects SQLite/Postgres by adapter, seeds the selected datastore, returns adapter proof metadata, and verifies Journey 7 custom-field persistence through a fresh Postgres adapter read. |
| 8 CI Postgres matrix | Done | 15adef3 | `node --test scripts/release-workflow.test.mjs` passed under Node 22 | Focused CI/spec review found two Medium issues: Postgres package metadata filter narrower than plan, and weak command guardrail. Both were fixed before commit. | Postgres CI now runs journeys 02-12 and 15 plus `src/harness/build-stack.test.ts`; release-sensitive path filters cover workflow files, release scripts, root/package metadata, package manifests, core/API/SDK/CLI/MCP/demo-seed sources, changesets, E2E, harnesses, and release definition docs. |
| 9 api_keys upsert safety | Done | b16da67 | `pnpm -F @orbit-ai/e2e typecheck`; `DATABASE_URL=postgres://localhost:5432/orbit_e2e ORBIT_E2E_ADAPTER=postgres pnpm -F @orbit-ai/e2e test src/harness/build-stack.test.ts`; `node --test scripts/release-workflow.test.mjs` passed under Node 22 | Security/tenant-safety review found no Critical/High/Medium findings and one Low teardown disconnect issue; fixed before commit | Postgres API-key setup now uses `ON CONFLICT (key_hash) DO NOTHING`, rejects cross-org hash collisions, does not update on key-prefix conflicts, restores prior SQLite build-stack smoke coverage, and static guardrails prevent reintroducing the unsafe conflict clauses. |
| 10 run-mcp leak fix | Done | ba52795 | `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/11-mcp-core-tools.test.ts`; `node --test scripts/release-workflow.test.mjs` passed under Node 22 | Focused code review found no findings | MCP harness now closes the server when client transport connection fails after server connection, preserves the original error, and has a static release-workflow guardrail for the cleanup path. |
| 11 Stripe sentinel | Done | 69224f8 | `pnpm -F @orbit-ai/integrations test`; `pnpm -F @orbit-ai/e2e test src/journeys/14-integrations-stripe.test.ts` passed under Node 22 | Security review found no findings | Stripe configure now stores `__orbit_sentinel__:stripe:api_key` as the API-key placeholder refresh token; status remains credential-presence based and tests assert the old sentinel is absent from runtime storage. |
| 12 Schema-engine org context | Done | 0143291 | `pnpm -F @orbit-ai/core typecheck`; `pnpm -F @orbit-ai/core test src/schema-engine/engine.test.ts`; `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/12-schema-read-parity.test.ts`; `ORBIT_E2E_ADAPTER=postgres DATABASE_URL=postgres://localhost:5432/orbit_e2e pnpm -F @orbit-ai/e2e test src/journeys/12-schema-read-parity.test.ts` passed under Node 22 | Tenant-safety/API-SDK-MCP parity review found one High and one Medium test-coverage issue; fixed by asserting both camel/snake custom-field shapes and proving beta can see `linkedin_url` before acme omission checks. Final review found no findings. | Schema reads now assert org context before repository access and missing org returns `AUTH_CONTEXT_REQUIRED`. Journey 12 covers raw API, SDK HTTP, SDK Direct, MCP `get_schema`, and MCP `orbit://schema` resource reads against shared tenant data. |
| 13 Deal value validation | Done | 184efdf | `pnpm -F @orbit-ai/core test src/entities/deals/validators.test.ts`; `pnpm -F @orbit-ai/core typecheck`; `pnpm -F @orbit-ai/sdk typecheck`; `pnpm -F @orbit-ai/sdk test -- resources-wave1 direct-transport`; `pnpm -F @orbit-ai/cli typecheck`; `pnpm -F @orbit-ai/cli test -- exit-codes`; `pnpm -r build`; `pnpm -F @orbit-ai/e2e typecheck`; `pnpm -F @orbit-ai/e2e test src/journeys/05-crud-deals.test.ts`; `ORBIT_E2E_ADAPTER=postgres DATABASE_URL=postgres://localhost:5432/orbit_e2e pnpm -F @orbit-ai/e2e test src/journeys/05-crud-deals.test.ts`; `pnpm --filter @orbit-ai/core test -- deals`; `rg -n "value:" packages/demo-seed --glob '*.ts'` returned no seed fixtures to update; `git diff --check` passed | Type/design review found SDK update could not type `value: null`; fixed with `DealValueInput` and reran focused verification. Spec/code review found no actionable code issues after the fix. Parity review found DirectTransport and CLI JSON error-shape drift; fixed with API-style direct error metadata defaults and CLI nested error-envelope preservation. Final parity re-review passed with no remaining findings. | Deal values now fit numeric(18,2), reject scientific notation/non-finite/unsafe values, preserve string decimals, normalize numeric inputs to two decimals, and expose SDK nullable update typing for clearing values. Journey 5 asserts `VALIDATION_FAILED` across raw API, SDK HTTP, SDK Direct, CLI JSON, and MCP, and asserts API-style error metadata for raw API, SDK HTTP, SDK Direct, and CLI JSON. |
| 14 Docs + changeset | Done | bc64ecd | `pnpm changeset status` passed; `rg -n "Journey 8|migration safety|tenant isolation|Postgres|stdio|connector|DirectTransport|custom fields|RLS|contacts and deals|Trusted Publishing|Dependabot|pnpm audit" docs/product/release-definition-v2.md e2e/README.md CHANGELOG.md` found the required limitation/evidence terms; release-facing stale workflow-sub-route search returned no matches; `git diff --check` passed | Docs/changeset review found the release-definition Journey 8 table still claimed reversible migration behavior; fixed to alpha stub passthrough wording. Re-review evidence: stale release-facing workflow-sub-route claims absent and new changeset contains the requested `@orbit-ai/core` and `@orbit-ai/integrations` patch entries. | `pnpm changeset status` also reports existing Plan B changesets for other packages; the new Plan C changeset itself only bumps `@orbit-ai/core` and `@orbit-ai/integrations`. `docs/product/release-definition-v2.md` is ignored by repo rules and was force-staged because Task 14 explicitly requires it. |
| 15 Verification + reviews | Done | 885831b | Final post-review reruns passed: `pnpm -r build`; `pnpm -r typecheck`; `pnpm -r lint`; `pnpm -r test`; `node --test scripts/release-workflow.test.mjs`; `pnpm changeset status`; `git diff --check`; focused SQLite/Postgres `pnpm -F @orbit-ai/e2e test src/journeys/05-crud-deals.test.ts`, `src/journeys/07-custom-field.test.ts`, `src/journeys/12-schema-read-parity.test.ts`, and `src/harness/build-stack.test.ts` variants passed; final local Postgres matrix `ORBIT_E2E_ADAPTER=postgres DATABASE_URL=postgres://localhost:5432/orbit_e2e pnpm -F @orbit-ai/e2e test src/journeys/02 src/journeys/03 src/journeys/04 src/journeys/05 src/journeys/06 src/journeys/07 src/journeys/08 src/journeys/09 src/journeys/10 src/journeys/11 src/journeys/12 src/journeys/15 src/harness/build-stack.test.ts` passed with 14 files / 18 tests. | Final parity review passed. Final database/Postgres review passed after rejecting large fractional JS numbers that can shift apparent cents. Final security and tenant-safety reviews passed. Final code review found two Low issues; fixed `.env.example` Socket variable and relabeled schema parity away from numbered Journey 12; re-review passed. | Post-review fixes restored Socket CI/release gates, restored Drizzle `^0.45.2`, aligned raw Postgres `deals.value` to `numeric(18,2)`, hardened same-org API-key hash collision handling, normalized DirectTransport schema response shapes, removed direct-mode deal workflow fallbacks, tightened money-number precision, and fixed review-documentation nits. |
| 16 PR | Pending |  |  |  |  |

## Deferred Items

- Real schema migration preview/apply engine: Plan C.5.
- DirectTransport custom-field update/delete engine methods: Plan C.5.
- Real Gmail/Calendar/Stripe provider contract tests: separate connector test plan.
- MCP stdio wire E2E: separate MCP transport plan.
- Restricted-role Postgres RLS proof: separate Postgres RLS regression plan.
- Tenant-isolation coverage beyond contacts and deals: separate broader entity-isolation plan.
- npm Trusted Publishing, Dependabot, and `pnpm audit` gating: deferred per Plan B follow-ups and tracked outside Plan C.
