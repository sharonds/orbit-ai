import { describe, it, expect } from 'vitest'

describe('SDK Wave 2 parity', () => {
  it.todo('activities.create returns same shape in both transports')
  it.todo('sequences.enroll calls correct path in both transports')
  it.todo('tags.attach calls correct path in both transports')
  it.todo('schema.listObjects returns same shape in both transports')
  it.todo('webhooks.deliveries returns AutoPager in both transports')
  it.todo('imports.create returns same shape in both transports')
  it.todo('sequence_events is read-only in both transports')
})
